import { SmartCasePipe } from "./smart-case.pipe";

describe("SmartCasePipe", () => {
  it("create an instance", () => {
    const pipe = new SmartCasePipe();
    expect(pipe).toBeTruthy();
  });
});
