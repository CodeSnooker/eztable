import { RowHostDirective } from './row-host.directive';

describe('RowHostDirective', () => {
  it('should create an instance', () => {
    const directive = new RowHostDirective();
    expect(directive).toBeTruthy();
  });
});
