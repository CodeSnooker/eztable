import { EzPadRightDirective } from './ez-pad-right.directive';

describe('EzPadRightDirective', () => {
  it('should create an instance', () => {
    const directive = new EzPadRightDirective();
    expect(directive).toBeTruthy();
  });
});
